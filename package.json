{
  "name": "ij-go-symbol-completion",
  "displayName": "Go Symbol Completion",
  "description": "IntelliJ-like Go symbol completion with or without package prefix",
  "version": "0.0.57",
  "publisher": "sttts",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Programming Languages",
    "Snippets",
    "Other"
  ],
  "activationEvents": [
    "onLanguage:go"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ij-go-symbol-completion.showSymbolCache",
        "title": "Go: Show Symbol Cache"
      },
      {
        "command": "ij-go-symbol-completion.reindexPackage",
        "title": "Go: Reindex Package"
      },
      {
        "command": "ij-go-symbol-completion.reindexPackageFromContext",
        "title": "Go: Reindex This Package"
      },
      {
        "command": "ij-go-symbol-completion.showPackageInfo",
        "title": "Go: Show Package Debug Info"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "when": "editorLangId == go && editorTextFocus",
          "command": "ij-go-symbol-completion.reindexPackageFromContext",
          "group": "navigation@1"
        }
      ],
      "commandPalette": [
        {
          "command": "ij-go-symbol-completion.reindexPackageFromContext",
          "when": "false"
        },
        {
          "command": "ij-go-symbol-completion.reindexPackage",
          "when": "editorLangId == go"
        }
      ]
    },
    "configuration": {
      "title": "Go Symbol Completion",
      "properties": {
        "goSymbolCompletion.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable/disable Go symbol completion"
        },
        "goSymbolCompletion.goplsPath": {
          "type": "string",
          "default": "gopls",
          "description": "Path to the gopls executable"
        },
        "goSymbolCompletion.debugLevel": {
          "type": "number",
          "default": 1,
          "enum": [0, 1, 2, 3],
          "enumDescriptions": [
            "No debugging output",
            "Basic debugging (errors and important operations)",
            "More detailed debugging (symbol extraction details)",
            "All debugging output (including command output)"
          ],
          "description": "Controls the verbosity of debug logging (0=none, 1=basic, 2=detailed, 3=verbose)"
        },
        "goSymbolCompletion.limitToDirectDeps": {
          "type": "boolean",
          "default": true,
          "description": "Limit indexing to only direct dependencies (recommended for better performance)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "postcompile": "mkdir -p dist && cp src/go_extract_symbols.go dist/",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "lint": "eslint src --ext ts",
    "package-vsix": "npm run compile && yes | vsce package --allow-missing-repository",
    "test": "mocha out/test/**/*.js"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/glob": "^7.1.3",
    "@types/mocha": "^9.1.1",
    "@types/node": "^14.14.37",
    "@types/vscode": "^1.60.0",
    "@typescript-eslint/eslint-plugin": "^5.13.0",
    "@typescript-eslint/parser": "^5.13.0",
    "chai": "^4.3.6",
    "eslint": "^7.32.0",
    "glob": "^7.1.7",
    "mocha": "^9.2.1",
    "ts-loader": "^9.2.8",
    "typescript": "^4.6.2",
    "webpack": "^5.70.0",
    "webpack-cli": "^4.9.2"
  }
}
